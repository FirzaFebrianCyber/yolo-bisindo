# 1. NEW: Upgrade Streamlit to allow Pillow 11.x (Streamlit 1.34.0+)
streamlit>=1.34.0

# 2. Unpin OpenCV to use the latest Python 3.13 compatible wheel
opencv-python-headless

# 3. Use Pillow 11.x, which has wheels for Python 3.13
pillow==11.0.0

# 4. Keep numpy unpinned to ensure wheel installation
numpy

# 5. Keep setuptools
setuptools>=69.0.3

# 6. Keep PyTorch version compatible with Python 3.13
torch==2.5.0

